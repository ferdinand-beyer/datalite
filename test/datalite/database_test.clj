(ns datalite.database-test
  (:require [clojure.test :refer :all]
            [datalite.connection :as conn]
            [datalite.database :refer :all]
            [datalite.schema :as schema]))

(deftest entity-exists-test
  )
